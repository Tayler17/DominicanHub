name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    needs: []
    steps:
      - uses: actions/checkout@v4

      - name: Deploy API
        run: echo "Configure your deployment target here (Railway, Render, AWS ECS...)"
        # Example for Railway:
        # - uses: bervProject/railway-deploy@main
        #   with:
        #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
        #     service: dominican-hub-api

      - name: Deploy Web
        run: echo "Vercel deployment happens automatically via Vercel GitHub integration"
